import { ModuleMenu } from "./ModuleMenu";
import ReactDOM from "react-dom";
import React from "react";

const showModuleMenu = () => {
  ReactDOM.render(<ModuleMenu />, element);
};

module.exports = showModuleMenu;
